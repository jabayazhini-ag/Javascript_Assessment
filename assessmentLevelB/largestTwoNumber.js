console.log("Finding the Largest Number using Two Input Variables:")

const pencil = 10;
const pen = 20;

if (pencil > pen) {
    console.log("The largest number is Pencil : " + pencil);
}
else if (pen>pencil) {
    console.log("The largest number is Pen : " + pen);
}
else {
    console.log("Both Pencil and Pen have the same value: " + pencil);
}